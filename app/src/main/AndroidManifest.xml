<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.dung.doantn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".sensor_detail.SensorInformation" >
    </activity>
    </application>

</manifest>
<!--     <activity android:name=".view.SensorFragment" />
        <activity android:name=".view.MapFragment" />
        <activity android:name=".view.OptionFragment" />
        <activity android:name=".sensor_detail.SensorInformation" />
        <activity android:name=".sensor_detail.InforChartFragment" />
        <activity android:name=".sensor_detail.SensorInforFragment">-->